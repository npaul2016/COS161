Map<Double, String> tGpa = new HashMap<Double, String>();
taGpa.put(3.6, new TreeSet<String>());
taGpa.get(3.6). add("Jared");
taGpa.put()

// abstract data types (ADTs) interface
// java collection framework
// and ADT can be implemented

// why use ADT ?
// why would you want more than one list, queue, etc. ?

// answer: they are implementing the same capabilities,



// Iterators: is an object that allows a client to traverse the element of any collection
// get the lement at that position
// advance to the next position
// remove the element at that position
// Examining sets and maps


//Iterators example

Iterator<Integer> itr = scores.Iterator();
while (itr.hasNext()) {

}
