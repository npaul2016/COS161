// write a method hat add a star

public static void addStars(ArrayList<String> list) {
  for (int )
}

// Object storing collections
// The compareTo method (10.2) :
//example:
public int compareTo (String other)

String a = "alice";
String b = "bob";
if (a.compareTo(b)) <0) { //true
  ...

}

// compareTo and collections > binary search
// You can use an array or list of strings with Javan's included binary search method
// because it calls compareTo internally.

String[] a = {"al" , "bob", "cari", "dan", "mike" }


int index = Arrays.binarySearch(a, "dan"); // 3

//Ordering our own types :

//comparable template

public class name implements Comparable<name> {

}

//Comparable example
public class Point implements Comparable<point> {
  private int x;
  private int y;
  ..
}

// sort by x and break ties by y

public int compareTo (Point other) {
  if (x !=other.x) {
    return x - other.x; // different x

  }
}


// Exercice > Make the HtmlTags 
